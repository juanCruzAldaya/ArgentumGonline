# Go build output
/server/bin/
*.exe

# Godot
/client/.godot/
/client/android/

# Export output. The web/ directory itself is tracked (empty) so that a fresh
# clone can `docker build` without having exported the client first.
/build/*
!/build/web/
/build/web/*
!/build/web/.gitkeep

# Editor / OS
.vscode/
.idea/
Thumbs.db
.DS_Store

# Python de un solo uso: overrides/apocalipsis.py deriva su PNG a mano y no
# corre en el build, pero deja su cache igual.
__pycache__/
